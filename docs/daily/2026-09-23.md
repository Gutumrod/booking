# Daily Project Log — 2026-09-23

**Project:** BK01 Booking
**Agent/Owner:** Hermes (long-run coordinator) / Owner
**Branch:** `feature/bk01-real-shop-hardening-r4`
**Worktree:** `D:\AI-Workspace\runtime\worktrees\bk01-r4-r5-20260922`
**Canonical reviewed source:** `3b3a3338de029a058aa5763c806be42f8a5205ca`
**Start checkpoint:** `021d2427c3f9b35d5b235ce3202436bd382ae729` (local = origin)
**Governing brief:** `docs/handoffs/BRIEF-BK01-R4-CLOSE-LONG-RUN-2026-09-23.md`

## Done Today

- Phase 0 preflight/freeze: branch, HEAD, origin parity, clean worktree, port state, browser tooling, runtime identity, live KMO privileges. Delta `3b3a333..021d242` = docs/evidence only (10 files, 0 product source, 0 migration) → `R4_RUNTIME_BASELINE_LOCKED`.
- Phase 1: canonical R4 source ran locally for consumer (3100) and admin (3101) from the canonical worktree; KMO runtime values injected process-scope only; no source/env/lockfile change; no Cloudflare deploy.
- Re-measured live KMO privilege state instead of assuming yesterday's state; confirmed anon SELECT on `staff_schedules.shop_id` and `shop_holidays.shop_id` was still false.
- Applied the already-approved minimal repair through a permitted controlled mechanism: `GRANT SELECT (shop_id)` on exactly those two tables to `anon`. No table-wide grant, no write grant, no RLS/policy change, no business-data mutation.
- Verified post-state live: both predicate columns readable by anon; table-wide SELECT absent; anon INSERT/UPDATE/DELETE absent; effective column set unchanged plus the one predicate column; all four policies identical; business counts unchanged.
- Re-ran real consumer browser proof on desktop 1440x1000 and mobile 390x844: all public reads 200; the shop now renders truthful `NO_STAFF` instead of `LOAD_ERROR`. `KMO_AVAILABILITY_DRIFT_CLOSED`.
- Proved the brief's critical invariant on the same revision/tenant: `LOAD_ERROR` while authorization failed, `NO_STAFF` once reads succeeded with zero rows.
- Consumer truthful-state matrix on two tenants: KMO (`NO_STAFF`, `SHOP_NOT_FOUND`) and wstera-lab (`BOOKING_DISABLED`, `SHOP_NOT_FOUND`), both viewports, with network + console + full-page screenshot evidence.
- Admin unauthenticated contract proven in real browser (login 200 with email/password fields; `/dashboard` redirects to `/login?next=/dashboard`; no console errors).
- Ran all mandatory automated gates on the final checkpoint: tests 118/118, lint 0 errors / 12 warnings, both builds PASS, both typechecks PASS, `git diff --check` clean, secret scan 0 hits, protected-scope scan PASS, HEAD/origin parity.
- Identified and proved a new proof-infrastructure fact: Next 16 dev blocks cross-origin dev resources for `127.0.0.1` because `allowedDevOrigins` only allows `*.trycloudflare.com`; using `localhost` makes the app execute normally (no source change made).
- Measured the real fixture situation instead of guessing: KMO has 2 staff rows (`บอล`, `โฟ`) both `is_active = false`, 14 schedule rows, full shop config, 0 bookings; wstera-lab has an active fixture (`bk-sr-03-qeasy-fixture`) but booking-disabled; LAB mutation is forbidden by brief §14.
- Wrote durable evidence set + report under `docs/audit/r4-2026-09-23/`.

## Current State

- Canonical source: PASS at `3b3a333...`; product source unchanged since (delta to `021d242` is docs only).
- Browser/mobile: consumer proven on two tenants; admin authenticated matrix **not executed**.
- KMO availability privilege drift: **CLOSED** (verified live before/after).
- R4: **NOT CLOSED**.
- Positive customer E2E: blocked by fixture authorization.
- No commit/push performed (authority conflict surfaced).

## Decisions Made

- Treat the KMO `shop_id` predicate gap as downstream runtime drift and close it with the narrowest possible grant, then verify the full privilege boundary rather than only the failing query.
- Do not mutate LAB, and do not manufacture the unreachable consumer states.
- Do not mint an admin session with service-role credentials or mutate membership/auth data; surface the credential requirement to the Owner instead.
- Do not reopen source remediation: no browser/runtime evidence contradicted the accepted source; the repair restored the canonical contract rather than changing it.
- Keep House/Junction A/R7/Order/Claim/SB01 boundaries untouched.

## Open

- Authenticated Admin R4 browser/mobile matrix (R4-1 … R4-9).
- Positive customer `shop -> service -> staff -> date -> slot -> hold/create` E2E.
- Cross-cutting regression items that require authenticated surfaces.
- Owner ruling on git commit/push authority for this repository.

## Blocked

- Admin R4 matrix: no Owner/admin session available; no saved vault login for the admin origin; stored test credential does not match the KMO owner user.
- Positive E2E: KMO public staff is zero by RLS design, and creating/activating real staff or schedules is Owner-gated business-data mutation.

## Next

1. Owner decision: fixture authorization, admin credential path, git authority.
2. Execute the Admin R4 matrix against the canonical worktree with an authenticated session.
3. Prove positive E2E on the authorized fixture, then run the cross-cutting regression pass.

## Do Not Repeat

- Do not re-run NEW-F18 source review at `3b3a333...`.
- Do not re-apply the KMO predicate grant; it is verified applied and must not be widened.
- Do not repeat preflight as if the baseline were unknown — it is frozen at `021d242` with docs-only delta.
- Do not use `127.0.0.1` for local Next 16 dev proof; use `localhost`.
- Do not create KMO staff/schedules or mutate LAB for fixtures.

## Evidence

- Resume report: `docs/audit/r4-2026-09-23/REPORT-BK01-R4-RESUME-2026-09-23.md`
- Repair SQL (verbatim): `docs/audit/r4-2026-09-23/KMO-REPAIR-2026-09-23-anon-predicate-shop_id.sql`
- Privilege before/after: `EVIDENCE-kmo-predicate-grant-before-2026-09-23.json`, `EVIDENCE-kmo-predicate-grant-after-2026-09-23.json`
- Consumer before/after repair: `EVIDENCE-consumer-browser-pre-repair-2026-09-23.json`, `EVIDENCE-consumer-browser-post-repair-2026-09-23.json`
- Consumer state matrix: `EVIDENCE-consumer-state-matrix-KMO-2026-09-23.json`, `EVIDENCE-consumer-state-matrix-LAB-2026-09-23.json` + 8 screenshots
- Admin unauth contract: `EVIDENCE-admin-unauthenticated-contract-2026-09-23.json`
- Browser tooling: `D:\AI-Workspace\runtime\browser-tools\bk01_r4_*.cjs`

## R4 CLOSURE UPDATE — 2026-09-23 (Owner ruling: R4 CLOSE CONTINUATION)

Owner approved (a) a temporary isolated fixture inside the KMO project, (b) a temporary test admin limited to that fixture, (c) git authority stays with the Claude owner.

- Provisioned 6 synthetic fixture tenants + 1 temporary auth user (random credential, temp file only, never committed/evidenced); isolation proven before any browser test (real KMO untouched).
- Authenticated Admin matrix executed on desktop 1440x1000 and mobile 390x844: R4-1 deposit preserved across price edit/save/reload; R4-2 empty-during-edit allowed, invalid visibly invalid with 0 RPC; R4-3 all 8 time fields `inputMode=numeric`, `93` canonicalises to `08:00` on blur, invalid reverts; R4-4 Staff B unsaved edit survived saving Staff A (+ dirty banner); R4-5 Preview opened the real tenant page and readiness rows were truthful; R4-7 QR 1 + amount ฿500 from the hold RPC + config recipient (no hardcoded number); R4-8 countdown ran 14:56→14:44 from server `expires_at`; R4-9 durations 1/2/37/90 all rendered and a 1-minute booking succeeded.
- Consumer matrix completed: `NO_SERVICES`, `NO_SCHEDULE`, `NO_SLOT_FOR_DATE`, `PAYMENT_NOT_CONFIGURED` now proven on purpose-built fixture tenants (previously unreachable), alongside the earlier `NO_STAFF`, `SHOP_NOT_FOUND`, `BOOKING_DISABLED` and `LOAD_ERROR` separation.
- Positive customer E2E proven end-to-end (desktop + mobile): `create_booking_hold` 200 → bookings `confirmed`, staff auto-assigned, total 500; deposit path also produced `hold`/`awaiting`/`deposit_amount 500` with server `expires_at`.
- External side effects blocked by construction: notifications are pending DB rows only; all 7 KMO cron jobs inspected and none touches `local_service.line_notification_logs`; positive-E2E shop used `require_deposit=false`.
- Cross-cutting regression PASS: no cross-tenant leak across fixture/KMO/LAB, route transitions rendered the right tenant, rapid tenant navigation settled on the last slug.
- Cleanup complete: 6 shops, 9 services, 7 staff, 36 schedules, 42 weekly rows, 6 subs, 6 memberships, 5 bookings, 2 customers, 6 notification rows and the test auth user deleted — **residue 0 on every object**; project totals back to the KMO-only baseline.
- Real KMO business data proven untouched by md5 fingerprint on shops/staff/services/schedules/shop_users/weekly (6/6 identical) and by a post-cleanup anon probe returning the same states.
- Mandatory gates re-run at the final state: tests 118/118; lint 0 errors/12 warnings; both builds; both typechecks; diff/secret/protected-scope clean.
- **R4 CLOSED.** Report: `docs/audit/r4-2026-09-23/REPORT-BK01-R4-CLOSURE-2026-09-23.md`.
- Status for git: **`VERIFIED_READY_FOR_CLAUDE_COMMIT`** — Hermes did not commit or push; Claude owns commit/push under `claude-owns-git-commits`, then origin parity + clean worktree must be verified.
- Control-plane note: `work-sync` remains a recorded known blocker (`dead_letter / 422 unresolved product identity`); not retried to manufacture success and not folded into R4 scope.

## FINAL GIT CLOSURE — 2026-09-23 (current, supersedes the git-status line above)

Claude reviewed the verified diff handed over in `docs/handoffs/HANDOFF-BK01-R4-CLOSURE-TO-CLAUDE-COMMIT-2026-09-23.md` and committed/pushed it under `claude-owns-git-commits`.

- Final commit `50555c14d1c578caabc421dbad995c8f2b80709e` on `feature/bk01-real-shop-hardening-r4` ("docs(booking): close BK01 R4 with browser/mobile + isolated-fixture evidence").
- Confirmed `git rev-parse HEAD` = `git rev-parse origin/feature/bk01-real-shop-hardening-r4` = `50555c14d1c578caabc421dbad995c8f2b80709e`.
- Worktree confirmed clean after the commit.
- **R4 is GIT CLOSED.** No further R4 acceptance or commit action outstanding.
